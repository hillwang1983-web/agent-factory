# Agent Workspace Rules

- **Do not push directly to GitHub**: When running git operations, merging branch fix/bug, or completing a task, always keep changes local. Propose branch updates or local merge commits for user review, and NEVER execute `git push` to origin or remote branches unless the user explicitly instructs you to do so.
